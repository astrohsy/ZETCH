import Typography from '@mui/material/Typography';

const Information = (props) => {
    //TODO: display review list
    //TODO: for every review, add buttons: 1) show replies(opens material-ui dialog to list repies of review) 2) reply (opens material-ui dialog to collect input)
    return (
        <Typography variant="h4" gutterBottom>
            Reviews
        </Typography>
    );
}

export default Information;


